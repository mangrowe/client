export const all = (state, teams) => {
    state.all = teams;
}

export const users = (state, users) => {
    state.users = users;
}

export const messages = (state, messages) => {
    state.messages = messages;
}